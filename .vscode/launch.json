{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Git Bash",
      "type": "python",
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal",
      "cwd": "${fileDirname}",
      "env": {},
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
